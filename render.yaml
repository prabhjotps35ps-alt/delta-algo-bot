services:

  - type: web

    name: omega-ai-trading-system

    env: docker

    plan: free

    autoDeploy: true

    healthCheckPath: /health

    envVars:

      - key: DELTA_API_KEY
        sync: false

      - key: DELTA_API_SECRET
        sync: false